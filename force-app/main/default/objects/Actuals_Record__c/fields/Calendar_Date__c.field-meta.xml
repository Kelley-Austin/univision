<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Calendar_Date__c</fullName>
    <label>Calendar Date</label>
    <type>Date</type>
    <required>false</required>
    <description>First day of the calendar month this actual covers. Populated when Date_Type__c = Calendar; null when Date_Type__c = Broadcast. Parsed from the Period column in the Counterpoint CSV (formats: MM/YYYY, YYYY-MM, or full ISO date).</description>
</CustomField>
