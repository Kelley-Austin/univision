<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Match_Key__c</fullName>
    <description>Composite external ID used for idempotent upsert. Format: BL|{BudgetLineId} for budget-line rows; ACT|{Channel}|{Vehicle}|{Month}|{Year} for unmatched actuals. Re-running reconciliation overwrites existing rows rather than creating duplicates.</description>
    <externalId>true</externalId>
    <label>Match Key</label>
    <length>255</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>true</unique>
</CustomField>
