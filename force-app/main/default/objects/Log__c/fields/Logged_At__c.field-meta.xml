<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Logged_At__c</fullName>
    <label>Logged At</label>
    <description>Datetime the event was logged. Stamped by Logger when the entry is added to the buffer (NOT when the buffer is flushed) so chronological ordering reflects when the event happened, not when DML committed.</description>
    <required>true</required>
    <type>DateTime</type>
</CustomField>
